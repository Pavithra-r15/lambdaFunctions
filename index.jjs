exports.handlerfour = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Welcome new user4!'),
    };
    return response;
};